{
  "name": "my-app-infra",
  "version": "1.0.0",
  "description": "Infrastructure for my-app",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "preview": "pulumi preview",
    "up": "pulumi up",
    "destroy": "pulumi destroy",
    "seed:zone": "npx tsx scripts/seed-zone.ts"
  },
  "dependencies": {
    "@pulumi/aws": "^6.0.0",
    "@pulumi/pulumi": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "mongodb": "^7.1.0",
    "typescript": "^5.0.0"
  }
}
