{
  "name": "my-app-infra",
  "version": "1.0.0",
  "description": "Infrastructure for my-app",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "preview": "pulumi preview",
    "up": "pulumi up",
    "destroy": "pulumi destroy",
    "seed:zone": "npx tsx --env-file=src/.env.local scripts/seed-zone.ts",
    "seed:bookings": "npx tsx --env-file=src/.env.local scripts/seed-bookings.ts",
    "migrate:service-type": "npx tsx --env-file=src/.env.local scripts/migrate-service-type.ts"
  },
  "dependencies": {
    "@pulumi/aws": "^6.0.0",
    "@pulumi/pulumi": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "mongodb": "^7.1.0",
    "typescript": "^5.0.0"
  }
}
